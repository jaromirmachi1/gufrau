{% liquid
  assign bg_image = section.settings.background_image
  assign logo_image = section.settings.logo
  assign ticket_label = section.settings.ticket_label | default: 'TICKETS'

  if section.settings.concerts != blank
    assign concerts = section.settings.concerts
  else
    assign concerts = shop.metaobjects.concert.values
  endif
%}

<section
  class="gufrau-shows"
  {{ section.shopify_attributes }}
>
  <div class="gufrau-shows__bg" aria-hidden="true">
    {%- if bg_image != blank -%}
      {{
        bg_image
        | image_url: width: 3840
        | image_tag:
          class: 'gufrau-shows__bg-img',
          widths: '800, 1200, 1600, 2000, 2400, 3200, 3840',
          sizes: '100vw',
          loading: 'eager',
          fetchpriority: 'high',
          alt: ''
      }}
    {%- else -%}
      <img
        class="gufrau-shows__bg-img"
        src="{{ 'eshpgufrau.jpg' | asset_url }}"
        alt=""
        width="1728"
        height="1117"
        loading="eager"
        fetchpriority="high"
      >
    {%- endif -%}
  </div>

  <a class="gufrau-shows__home" href="{{ routes.root_url }}">home</a>

  <div class="gufrau-shows__stage">
    {%- if concerts != blank and concerts.size > 0 -%}
      <ul class="gufrau-shows__list" role="list">
        {%- for entry in concerts -%}
          {%- liquid
            comment
              Works for both metaobject_list entries and shop.metaobjects.concert.values
            endcomment
            assign show = entry
            if entry.system != blank
              assign show = entry
            endif

            assign show_date = show.date.value | default: show.date
            assign show_city = show.city.value | default: show.city
            assign show_venue = show.venue.value | default: show.venue
            assign show_url = show.ticket_url.value | default: show.ticket_url

            if show_date == blank and show_city == blank and show_venue == blank
              continue
            endif

            assign date_label = show_date
            if show_date != blank
              assign date_check = show_date | date: '%Y'
              if date_check != blank and date_check != '0'
                assign date_label = show_date | date: '%d.%m.%Y'
              endif
            endif

            assign location_label = ''
            if show_city != blank and show_venue != blank
              assign location_label = show_city | append: ' - ' | append: show_venue
            elsif show_city != blank
              assign location_label = show_city
            elsif show_venue != blank
              assign location_label = show_venue
            endif
          -%}
          <li class="gufrau-shows__row">
            <span class="gufrau-shows__date">{{ date_label | escape }}</span>
            <span class="gufrau-shows__location">{{ location_label | upcase | escape }}</span>
            {%- if show_url != blank -%}
              <a
                class="gufrau-shows__tickets"
                href="{{ show_url }}"
                target="_blank"
                rel="noopener noreferrer"
              >
                {{ ticket_label | escape }}
              </a>
            {%- else -%}
              <span class="gufrau-shows__tickets gufrau-shows__tickets--disabled" aria-disabled="true">
                {{ ticket_label | escape }}
              </span>
            {%- endif -%}
          </li>
        {%- endfor -%}
      </ul>
    {%- elsif request.design_mode -%}
      <p class="gufrau-shows__empty">
        Add concerts in Shopify Admin → Content → Metaobjects → Concert
      </p>
    {%- endif -%}
  </div>

  <div class="gufrau-shows__logo">
    {%- if logo_image != blank -%}
      {{
        logo_image
        | image_url: width: 2400
        | image_tag:
          class: 'gufrau-shows__logo-img',
          widths: '600, 900, 1200, 1600, 2000, 2400',
          sizes: '100vw',
          loading: 'eager',
          alt: shop.name
      }}
    {%- else -%}
      <img
        class="gufrau-shows__logo-img"
        src="{{ 'gfraushows.png' | asset_url }}"
        alt="{{ shop.name | escape }}"
        width="1807"
        height="425"
        loading="eager"
      >
    {%- endif -%}
  </div>
</section>

{% stylesheet %}
  body:has(.gufrau-shows) {
    background: #0a0a0a;
    color: #fff;
  }

  body:has(.gufrau-shows) #header-group:empty,
  body:has(.gufrau-shows) footer:has(.shopify-section-group-footer-group:empty) {
    display: none;
  }

  body:has(.gufrau-shows) .content-for-layout {
    display: flex;
    flex-direction: column;
    min-height: 100dvh;
  }

  body:has(.gufrau-shows) .content-for-layout > .shopify-section:has(.gufrau-shows) {
    flex: 1;
    display: flex;
  }

  .gufrau-shows {
    --gufrau-ease: cubic-bezier(0.23, 1, 0.32, 1);
    --gufrau-tickets-bg: #0000ff;
    position: relative;
    flex: 1;
    width: 100%;
    min-height: 100dvh;
    height: 100dvh;
    max-height: 100dvh;
    display: grid;
    grid-template-rows: 1fr auto;
    isolation: isolate;
    overflow: hidden;
    color: #fff;
  }

  .gufrau-shows__bg {
    position: absolute;
    inset: 0;
    z-index: 0;
    width: 100%;
    height: 100%;
  }

  .gufrau-shows__bg-img {
    display: block;
    width: 100%;
    height: 100%;
    object-fit: cover;
    object-position: center center;
  }

  .gufrau-shows__home {
    position: absolute;
    top: clamp(1rem, 3vw, 1.75rem);
    left: clamp(1rem, 3vw, 1.75rem);
    z-index: 3;
    font-size: 0.8125rem;
    font-weight: 600;
    letter-spacing: 0.08em;
    text-transform: lowercase;
    text-decoration: none;
    color: #fff;
    mix-blend-mode: difference;
    transition: opacity 180ms var(--gufrau-ease);
  }

  .gufrau-shows__home:hover {
    opacity: 0.7;
  }

  .gufrau-shows__stage {
    position: relative;
    z-index: 2;
    display: flex;
    align-items: center;
    justify-content: center;
    min-height: 0;
    padding: clamp(4rem, 10vh, 6rem) clamp(1rem, 4vw, 2.5rem) 0;
    overflow: hidden;
  }

  .gufrau-shows__list {
    list-style: none;
    margin: 0;
    padding: 0 0 18vh;
    width: min(100%, 52rem);
    max-height: calc(100dvh - 28vh);
    overflow-x: hidden;
    overflow-y: auto;
    overscroll-behavior: contain;
    scrollbar-width: none;
    -webkit-mask-image: linear-gradient(
      to bottom,
      #000 0%,
      #000 62%,
      transparent 96%
    );
    mask-image: linear-gradient(
      to bottom,
      #000 0%,
      #000 62%,
      transparent 96%
    );
  }

  .gufrau-shows__list::-webkit-scrollbar {
    display: none;
  }

  .gufrau-shows__row {
    display: grid;
    grid-template-columns: 7.5rem minmax(0, 1fr) auto;
    gap: 0.75rem 1.25rem;
    align-items: center;
    padding: 0.55rem 0;
    font-size: clamp(0.875rem, 1.35vw, 1.0625rem);
    font-weight: 600;
    letter-spacing: 0.01em;
    line-height: 1.2;
  }

  .gufrau-shows__date {
    font-variant-numeric: tabular-nums;
    white-space: nowrap;
  }

  .gufrau-shows__location {
    min-width: 0;
    overflow: hidden;
    text-overflow: ellipsis;
    white-space: nowrap;
  }

  .gufrau-shows__tickets {
    display: inline-flex;
    align-items: center;
    justify-content: center;
    min-height: 2rem;
    padding: 0.4rem 1rem;
    border-radius: 999px;
    background: var(--gufrau-tickets-bg);
    color: #fff;
    font-size: 0.75rem;
    font-weight: 700;
    letter-spacing: 0.06em;
    text-decoration: none;
    text-transform: uppercase;
    white-space: nowrap;
    transition:
      transform 160ms var(--gufrau-ease),
      filter 160ms var(--gufrau-ease);
  }

  a.gufrau-shows__tickets:hover {
    filter: brightness(1.08);
    transform: scale(1.02);
  }

  a.gufrau-shows__tickets:active {
    transform: scale(0.97);
  }

  .gufrau-shows__tickets--disabled {
    opacity: 0.45;
    pointer-events: none;
  }

  .gufrau-shows__empty {
    margin: 0;
    padding: 1rem;
    text-align: center;
    font-size: 0.875rem;
    opacity: 0.8;
  }

  .gufrau-shows__logo {
    position: absolute;
    left: 0;
    right: 0;
    bottom: 0;
    z-index: 1;
    width: 100%;
    margin: 0;
    padding: 0;
    line-height: 0;
    pointer-events: none;
  }

  .gufrau-shows__logo-img {
    transform: translateY(10%);
    display: block;
    width: 100%;
    max-width: none;
    height: auto;
    margin: 0;
  }

  @media screen and (max-width: 749px) {
    .gufrau-shows__row {
      grid-template-columns: 1fr auto;
      grid-template-areas:
        'date tickets'
        'location tickets';
      gap: 0.15rem 0.85rem;
      padding: 0.7rem 0;
    }

    .gufrau-shows__date {
      grid-area: date;
      font-size: 0.8125rem;
      opacity: 0.9;
    }

    .gufrau-shows__location {
      grid-area: location;
      white-space: normal;
    }

    .gufrau-shows__tickets {
      grid-area: tickets;
      align-self: center;
    }

    .gufrau-shows__list {
      padding-bottom: 22vh;
      max-height: calc(100dvh - 24vh);
    }
  }
{% endstylesheet %}

{% schema %}
{
  "name": "GUFRAU shows",
  "tag": "div",
  "class": "gufrau-shows-section",
  "settings": [
    {
      "type": "paragraph",
      "content": "Shows load from the Concert metaobject (type: concert). Create it in Admin → Content → Metaobjects. Fields: date, city, venue, ticket_url. Enable storefront access."
    },
    {
      "type": "metaobject_list",
      "id": "concerts",
      "label": "Concerts",
      "metaobject_type": "concert",
      "info": "Optional. Leave empty to show all Concert entries automatically."
    },
    {
      "type": "text",
      "id": "ticket_label",
      "label": "Ticket button label",
      "default": "TICKETS"
    },
    {
      "type": "image_picker",
      "id": "background_image",
      "label": "Background image",
      "info": "Defaults to eshpgufrau.jpg if empty"
    },
    {
      "type": "image_picker",
      "id": "logo",
      "label": "Bottom logo",
      "info": "Defaults to gfraushows.png if empty"
    }
  ],
  "presets": [
    {
      "name": "GUFRAU shows"
    }
  ]
}
{% endschema %}
